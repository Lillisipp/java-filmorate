logging:
  level:
    org:
      zalando:
        logbook: TRACE

spring:
  datasource:
    url: jdbc:h2:file:./db/filmorate;MODE=PostgreSQL
    driver-class-name: org.h2.Driver
    username: sa
    password: password
  sql:
    init:
      mode: always
  h2:
    console:
      enabled: true
      path: /h2-console

